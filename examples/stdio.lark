// A module that declares the parts of the C standard input and output that
// Lark code uses. Rule N-6 needs `export` on every symbol that another module
// can name.

export int printf(const char* restrict format, ...);
